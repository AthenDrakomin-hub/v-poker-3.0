/**
 * 房间处理锁
 *
 * 全局共享的内存级锁，用于防止同一房间被并发处理：
 * - 玩家行动路由（PUT /:id/hand）
 * - 超时自动行动检查器
 *
 * 注意：这是单进程内存级锁，多实例部署时需要配合数据库行锁使用。
 */

/** 正在处理的房间 ID 集合 */
export const processingRooms = new Set<number>();
